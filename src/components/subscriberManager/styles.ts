// src/components/subscriberManager/styles.ts
import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  container: {
    display: 'flex',
    alignItems: 'center',
    marginBottom: '20px',
  },
  title: {
    fontSize: '24px',
    marginRight: '20px',
  },
  button: {
    marginLeft: '40px',
  },
});

export default useStyles;
